export type ParamsCatchAll = {
  params: {
    slug: string[];
  };
};
export type SlideShowProps = { images: string[] };
export type Project = {
  title: string;
  description: string;
  images: string[];
  link: string;
};
